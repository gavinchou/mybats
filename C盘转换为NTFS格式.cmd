@ ECHO OFF
@ ECHO.
@ ECHO.                             说   明
@ ECHO --------------------------------------------------------------------------
@ ECHO NTFS是一种磁盘格式。该格式能存放大于4G的单个文件(如电影文件),并可对文件夹
@ ECHO 进行加密,但有个缺点是DOS下无法访问。通常用户不会把巨型文件或重要文件放C盘，
@ ECHO 因此我们建议C盘不转换，仍用FAT32格式，以便需要时可以进DOS修理系统。
@ ECHO --------------------------------------------------------------------------
@ ECHO.
pause
::convert c:/fs:ntfs
